/**
 * Summary Component
 * Displays hardware profile and selection statistics
 *
 * CSS: @import './src/components/summary/summary.styles.css' layer(components);
 */

export {
  getHardwareProfile,
  getSelectedOptimizations,
  setupFormListeners,
  updatePreflightCheck,
  updateSummary,
} from './summary'
